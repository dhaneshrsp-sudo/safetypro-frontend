const fs=require("fs");const path="C:/safetypro_complete_frontend/safetypro_audit_compliance.html";let c=fs.readFileSync(path,"utf8");const lines=c.split("\n");for(let i=10100;i<10115;i++){if(lines[i]&&lines[i].includes("join(")&&lines[i][lines[i].length-1]===String.fromCharCode(39)){console.log("Found at L"+(i+1));lines[i]=lines[i]+"\\n"+lines[i+1];lines[i+1]="";break;}}c=lines.join("\n");fs.writeFileSync(path,Buffer.from(c,"utf8"));console.log("Done size:",c.length);