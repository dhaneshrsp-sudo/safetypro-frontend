const fs=require("fs");const path="C:/safetypro_complete_frontend/safetypro_audit_compliance.html";let c=fs.readFileSync(path,"utf8");const lines=c.split("\n");let fixed=0;// Fix all unclosed single-quoted strings by joining with next linefor(let i=9679;i<10300;i++){if(!lines[i])continue;let q=0;for(let j=0;j<lines[i].length;j++){let ch=lines[i][j];if(ch===String.fromCharCode(39)&&(j===0||lines[i][j-1]!==String.fromCharCode(92)))q++;}if(q%2!==0&&i+1<lines.length){console.log("Fixing L"+(i+1));lines[i]=lines[i]+String.fromCharCode(92)+"n"+lines[i+1];lines[i+1]="";fixed++;}}console.log("Fixed:",fixed);c=lines.join("\n");fs.writeFileSync(path,Buffer.from(c,"utf8"));console.log("Size:",c.length);